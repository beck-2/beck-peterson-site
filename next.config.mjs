// ABOUTME: Next.js configuration for the site.
// ABOUTME: No custom options needed yet — defaults cover this project.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
